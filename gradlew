#!/bin/sh
# Gradle start up script. Open this project in Android Studio and let it
# regenerate the full gradle wrapper (Studio does this automatically),
# or run: gradle wrapper --gradle-version 8.6
echo "Please open this project in Android Studio to auto-generate the Gradle wrapper,"
echo "or run 'gradle wrapper --gradle-version 8.6' if you have Gradle installed."
